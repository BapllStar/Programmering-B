let w
let wSize = 500
let h
let hSize = 500

function setup() {
  createCanvas(wSize, hSize);
  background(255);
}

function draw() {
  //console.log("test")
  //w++; h++;
  //resizeCanvas(sin(w)*wSize, cos(h)*hSize);
}
